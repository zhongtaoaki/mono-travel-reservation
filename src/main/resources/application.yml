server:
  port: 8080
spring:
  datasource:
    url: jdbc:h2:mem:samplemsadb
    driverClassName: org.h2.Driver
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
logging:
  level:
    org:
      hibernate:
        SQL: debug
      

mono-hotel-host: http://mono-hotel
mono-flight-host: http://mono-flight
